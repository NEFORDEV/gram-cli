[project]
name = "gram-cli"
version = "0.1.1"
description = "CLI утилита Gram для анализа Python-кода"
authors = [
    { name = "NEFOR" }
]
requires-python = ">=3.8"
dependencies = [
    "click>=8.0",
    "requests>=2.25.0",
    "toml>=0.10.0",
    "psutil>=5.8.0",
    "rich>=12.0.0"
]


[project.scripts]
gram = "gram.__main__:cli"
